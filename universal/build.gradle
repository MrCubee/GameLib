plugins {
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

jar {
    enabled = false;
    dependsOn(shadowJar { classifier = null })
}

dependencies {
    implementation project(':gamelib-kit')
}